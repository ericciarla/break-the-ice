// Needs work

class pFilters {
  int minAge;
  int maxAge;
  int maxDist;
  String peopleFilter;
  pFilters(
      {required this.minAge,
      required this.maxAge,
      required this.maxDist,
      required this.peopleFilter});
}
